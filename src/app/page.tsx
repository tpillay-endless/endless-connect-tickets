import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Endless Connect',
};

// renders a blank page at /tickets
export default function Home() {
  return null; // nothing
}
